'use client';

import React from 'react';
import HomeContent from '@/pages/Home'; // We reuse the existing logic, assuming it's refactored for Next.js Links

export default function HomePage() {
  return <HomeContent />;
}